# Server Configuration
PORT=3001
NODE_ENV=development

# Database
# SQLite database will be created in ./data/monipx.db

# SSH Terminal Security
# IMPORTANT: Generate a secure 32-byte hex key for production
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SSH_ENCRYPTION_KEY=your-secure-32-byte-hex-key-here

# Session Secret (for future authentication features)
SESSION_SECRET=your-session-secret-here

# CORS Settings (for production)
# CORS_ORIGIN=https://yourdomain.com

